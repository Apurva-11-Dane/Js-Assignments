// Create the user object
let user = {
    name: "Apurva",
    age: 21,
    city: "Aurangabad"
};

// Update the age property
user.age = 22;

// Add a new property: profession
user.profession = "Intern";

// Delete the city property
delete user.city;

// Print the final object
console.log(user);
